namespace neio.lang;

class Content
implements neio.lang.Texable;
List<Content> content;

Content() {
    content = new ArrayList<Content>();
}

String toTex() {
	String tex = "";
	for (int i = 0; i < content.size(); i = i + 1) {
		tex = tex + content.get(i).toTex();
	}
	
	return tex;
}
