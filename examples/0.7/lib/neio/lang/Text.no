namespace neio.lang;

class Text extends Content implements Comparable<String>;

String text;

Text(String text) {
    this.text = text;
}

String toString() {
    return text;
}

int compareTo(String o) {
    return text.compareTo(o);
}

String toTex() {
	return text;
}
