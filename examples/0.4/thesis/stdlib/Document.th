extend thesis.lang.BaseContainer;

Text title;

#(String title, Integer level) {
	new Chapter(title, level) chapter;
	content.add(chapter);
	
	return chapter;
}

newline(String parText) {
	new Paragraph(parText) p;
	content.add(p);
	
	return p;
}